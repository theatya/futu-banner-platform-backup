# 0912 版本

## 第一步：识别画板

- 主视觉组件和母版画板分别通过 Figma 链接识别，并使用串行队列避免并发冲突。
- 支持识别中、排队中、耗时、取消及重新识别状态。
- 主视觉组件优先识别；重新识别组件后自动刷新母版画板。
- 通过 Figma `componentId` 关联母版中的主视觉实例。
- 三栏工作区展示主视觉预览、母版预览和图层映射。
- 仅高亮当前选中的母版图层；主视觉实例不进入普通图层映射。
- 支持预设角色、排除图层和自定义图层识别。
- 映射结果按行显示“已映射”，图层较多时右侧面板可滚动。
- 自动识别母版背景色，支持 Figma 风格色板、HEX 输入和系统吸管。
- 背景色随母版引用保存，为后续延展画幅的背景填充提供依据。
- 兼容没有背景色字段的旧任务缓存。

## Figma 写回连接器

- 平台为主操作界面，Figma 插件仅作为轻量画布连接器。
- 使用稳定的 8 位配对码建立平台与插件连接。
- 写入任务使用文件持久化队列，避免开发服务器刷新导致任务丢失。
- 插件轮询本地写入接口，在当前 Figma 文件创建页面、画板和图层。
- 主视觉以 Component Instance 写入，保留组件关系。
- 当前开发阶段需运行本地平台并保持开发插件开启。
- 正式发布时应部署平台，并把连接器发布为团队内部插件；用户仅需完成一次安装和授权。
- 当前仅保证主视觉组件与写入目标位于同一 Figma 文件；跨文件团队库组件导入暂未接入。

## 交互与视觉

- 工作流按钮统一为紧凑的品牌橙渐变样式。
- 第一步右下角操作统一为“上一步 / 确认映射”，首步的“上一步”禁用。
- 减少外层套框、重复标题和非必要分割线。
- 默认 100% 浏览器缩放采用此前 150% 的视觉尺度，并保持全屏布局。

## 移植

第一步的完整行为说明、关键文件和检查项见：

`docs/STEP-1-RECOGNIZE-BOARD-HANDOFF.md`
